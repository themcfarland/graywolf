source "https://rubygems.org"

# fastlane drives Play Store track promotion and store-listing image
# uploads via `supply`. See fastlane/Fastfile. Only used by the Android
# release tooling (CI + `make android-promote` / `make android-upload-listing`);
# nothing in the graywolf runtime depends on Ruby.
gem "fastlane"
