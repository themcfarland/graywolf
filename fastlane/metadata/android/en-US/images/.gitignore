# Store-listing images are generated by `make android-screenshots` and
# staged here at upload time; don't commit the binaries. Keep the dir
# structure (.gitkeep) so fastlane supply finds the expected folders.
*.png
*.jpg
*.jpeg
